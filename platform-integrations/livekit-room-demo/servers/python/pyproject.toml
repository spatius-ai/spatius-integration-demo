[project]
name = "livekit-room-example-backend"
version = "0.1.0"
description = "Token server for the LiveKit Room example demo"
requires-python = ">=3.10,<3.15"
dependencies = [
  "flask>=3.0.0",
  "flask-cors>=4.0.0",
  "livekit-api>=1.1.0",
  "python-dotenv>=1.0.0",
]
