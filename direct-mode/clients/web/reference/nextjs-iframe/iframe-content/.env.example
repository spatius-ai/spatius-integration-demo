# Where the Direct Mode server lives. Optional: without it the client uses the page's
# own host on port 8090, which is right when both run on the same machine.
#
# Set it when the server is elsewhere — another machine on the LAN, or a phone
# reaching your laptop:
# VITE_DIRECT_MODE_URL=http://192.168.1.10:8090
