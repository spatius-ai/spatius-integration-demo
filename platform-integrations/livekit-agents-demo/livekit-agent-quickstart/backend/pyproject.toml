[project]
name = "livekit-agent-quickstart-backend"
version = "0.1.0"
requires-python = ">=3.10,<3.15"
dependencies = [
  "flask>=3.0.0",
  "flask-cors>=4.0.0",
  "python-dotenv>=1.0.0",
  "livekit-api>=1.1.0",
  # Keep livekit-agents, livekit-plugins-google, and livekit-plugins-spatius
  # pinned together so the avatar plugin tracks the matching agents release.
  "livekit-agents==1.5.17",
  "livekit-plugins-google==1.5.17",
  "livekit-plugins-spatius==1.5.17",
]
